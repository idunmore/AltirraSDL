AltirraSDL — Audio Output Parity Checklist
==========================================
Updated: 2026-04-02
Reference: PORTING/AUDIO.md vs actual audiooutput_sdl3.cpp

Legend:
  [100%] Fully implemented
  [ 50%] Partial / basic implementation
  [  0%] Not implemented


================================================================================
1. ATAUDIOOUTPUTSDL3 CLASS
================================================================================

  [100%] Class exists                         — ATAudioOutputSDL3 : public IATAudioOutput, public IATAudioMixer
  [100%] Factory function                     — ATCreateAudioOutput() returns SDL3 instance
  [100%] Build integration                    — Compiled via AltirraSDL/CMakeLists.txt
  [100%] Destructor                           — SDL_DestroyAudioStream cleanup

  STATUS: Full mixer implementation matching Windows ATAudioOutput pipeline.


================================================================================
2. CORE AUDIO PIPELINE
================================================================================

  [100%] SDL_AudioStream creation             — SDL_OpenAudioDeviceStream in InitNativeAudio()
  [100%] Audio device open + bind             — SDL_OpenAudioDeviceStream handles both
  [100%] WriteAudio() full pipeline           — Sync sources → edges → DC removal → LPF → async → SDL3
  [100%] Resampling                           — SDL3 auto-resamples from POKEY rate to device rate
  [100%] Dynamic rate change                  — SetCyclesPerSecond updates stream format + player rates
  [100%] Audio device shutdown                — Destructor calls SDL_DestroyAudioStream
  [100%] InitNativeAudio() called             — Added to main_sdl3.cpp after settings load


================================================================================
3. IATAudioOutput INTERFACE METHODS
================================================================================

  [100%] Init(ATScheduler&)                   — Creates sample players, edge player, configures rates
  [100%] InitNativeAudio()                    — Creates SDL3 audio stream at POKEY rate
  [100%] GetApi() / SetApi()                  — Returns Auto / no-op (correct for SDL3)
  [100%] AsMixer()                            — Returns *this (real mixer, not stub)
  [100%] GetPool()                            — Returns sample pool reference
  [100%] SetCyclesPerSecond()                 — Updates mixing rate, stream format, player rates
  [100%] GetMute() / SetMute()               — Flag applied in output stage
  [100%] GetVolume() / SetVolume()            — Applied through ATAudioFilter::SetScale (matches Windows)
  [100%] GetMixLevel() / SetMixLevel()        — Per-bus volume stored, passed to sources via mixInfo
  [100%] GetLatency() / SetLatency()          — Clamped [10,500], used for queue management
  [100%] GetExtraBuffer() / SetExtraBuffer()  — Clamped [10,500], used for queue management
  [100%] SetFiltersEnabled()                  — Calls ATAudioFilter::SetActiveMode on both channels
  [100%] Pause() / Resume()                   — SDL_PauseAudioStreamDevice / Resume with ref counting
  [100%] WriteAudio()                         — Full mixing pipeline with chunk splitting
  [100%] GetAudioStatus()                     — Reports rates, under/overflow, stereo, drop counts
  [100%] SetAudioTap()                        — Raw filtered audio forwarded to tap


================================================================================
4. IATAUDIOMIXER INTERFACE METHODS
================================================================================

  [100%] AddSyncAudioSource()                 — Registered sources mixed in WriteAudio pipeline
  [100%] RemoveSyncAudioSource()              — Proper removal from source list
  [100%] AddAsyncAudioSource()                — Mixed at POKEY rate into filtered output
  [100%] RemoveAsyncAudioSource()             — Proper removal from source list
  [100%] GetSamplePlayer()                    — Real ATAudioSamplePlayer (disk/printer sounds)
  [100%] GetEdgeSamplePlayer()                — Real ATAudioSamplePlayer (speaker click convolution)
  [100%] GetEdgePlayer()                      — Real ATSyncAudioEdgePlayer (speaker/covox/cassette edges)
  [100%] GetAsyncSamplePlayer()               — Real ATAudioSamplePlayer at mixing rate
  [100%] AddInternalAudioTap()                — Internal taps receive raw POKEY before filtering
  [100%] RemoveInternalAudioTap()             — Proper removal
  [100%] BlockInternalAudio()                 — Ref-counted blocking (zeroes POKEY in source buffer)
  [100%] UnblockInternalAudio()               — Ref-counted unblocking


================================================================================
5. AUDIO FEATURES
================================================================================

  [100%] Stereo output                        — Dynamic mono/stereo switching matching Windows
  [100%] Mono fallback                        — Automatic stereo→mono after kBufferSize mono samples
  [100%] Volume control                       — Through ATAudioFilter::SetScale (not raw multiply)
  [100%] Mute                                 — Zeroes output in interleave stage
  [100%] Pause / Resume                       — SDL3 device pause/resume with ref counting
  [100%] Audio tap (recording)                — Filtered audio forwarded after async mixing
  [100%] Latency control                      — Queue depth monitoring with overflow drop logic
  [100%] Audio status reporting               — Incoming rate profiling, under/overflow/drop counts
  [100%] Audio filters (DC removal + LPF)     — ATAudioFilter PreFilterDiff/PreFilterEdges/Filter
  [100%] Mix-bus volume application           — Levels passed via ATSyncAudioMixInfo::mpMixLevels
  [100%] Real mixer (sync audio sources)      — Full pipeline: POKEY → sync → edges → filter → async
  [100%] Edge player (speaker/covox/cassette) — ATSyncAudioEdgePlayer with triangle filtering
  [100%] Stock audio samples                  — 11 PCM files embedded, loaded via ATLoadMiscResource
  [100%] Sample player (disk/printer sounds)  — ATAudioSamplePlayer with convolution support
  [100%] Turbo mode queue management          — Drops blocks when SDL3 queue exceeds threshold
  [100%] Internal audio tap                   — Raw POKEY forwarded before mixing/filtering


================================================================================
6. REMAINING WORK
================================================================================

  All audio mixer functionality is implemented. No remaining items.


================================================================================
SUMMARY
================================================================================

Category                    Implemented    Total    Coverage
------------------------------------------------------------
SDL3 audio pipeline              7            7      100%
Interface methods (Output)      16           16      100%
Interface methods (Mixer)       12           12      100%
Audio features                  16           16      100%

Full audio mixer with parity to Windows ATAudioOutput:
- POKEY audio with sync source mixing (disk drives, speakers, printers, etc.)
- Edge player for speaker clicks, covox, cassette audio
- DC removal high-pass filter + low-pass anti-aliasing filter
- Dynamic mono/stereo switching
- Stock audio samples embedded (11 PCM files)
- Turbo mode overflow protection
- Audio recording tap support
