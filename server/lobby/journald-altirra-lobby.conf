# Drop-in for /etc/systemd/journald.conf.d/ on the lobby VM.
# Caps persistent journal size so a crash-loop or log flood can't
# fill the 20 GB free-tier disk.  Installed by the deploy pipeline.
[Journal]
SystemMaxUse=200M
SystemKeepFree=500M
